apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - spring-boot/
  - fastapi/
  - ingress-api.yaml
  - ingress-ws.yaml
  - networkpolicy.yaml

labels:
  - pairs:
      managed-by: argocd
      project: tasteam
