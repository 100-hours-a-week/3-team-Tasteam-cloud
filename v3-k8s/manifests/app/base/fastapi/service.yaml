apiVersion: v1
kind: Service
metadata:
  name: fastapi-svc
  labels:
    app: fastapi
spec:
  type: ClusterIP
  selector:
    app: fastapi
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
