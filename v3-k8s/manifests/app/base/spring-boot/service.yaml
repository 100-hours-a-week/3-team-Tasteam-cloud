apiVersion: v1
kind: Service
metadata:
  name: spring-boot-svc
  labels:
    app: spring-boot
spec:
  type: ClusterIP
  selector:
    app: spring-boot
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
